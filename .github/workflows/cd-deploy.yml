name: CD - Build, Push & Deploy

on:
  push:
    branches: [ "main" ]   # אפשר לשנות ל-branch אחר אם תרצה

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/devops-cd-demo:latest
          # למשל: galgdev/devops-cd-demo:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 129.159.144.185
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd app-for-CD
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/devops-cd-demo:latest
            docker compose down
            docker compose up -d
